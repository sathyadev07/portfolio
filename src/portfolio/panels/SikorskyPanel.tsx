import EntryPanel from '../EntryPanel';
import type {OpenDetail} from '../PanelShell';
export default function SikorskyPanel({onOpen}:{onOpen:OpenDetail}){return <EntryPanel id="sikorsky" onOpen={onOpen}/>;}
