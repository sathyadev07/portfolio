import EntryPanel from '../EntryPanel';
import type {OpenDetail} from '../PanelShell';
export default function FormulaSaePanel({onOpen}:{onOpen:OpenDetail}){return <EntryPanel id="fsae" onOpen={onOpen}/>;}
