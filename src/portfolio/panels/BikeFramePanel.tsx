import EntryPanel from '../EntryPanel';
import type {OpenDetail} from '../PanelShell';
export default function BikeFramePanel({onOpen}:{onOpen:OpenDetail}){return <EntryPanel id="proj-01" onOpen={onOpen}/>;}
